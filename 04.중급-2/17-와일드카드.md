# 🧠 Kotlin의 제네릭 타입 공변성과 반공변성
`out`과 `in`은 Kotlin의 제네릭 타입에서 읽기 전용과 쓰기 전용을 구분하는 키워드입니다.
Java의 ? `extends T` → Kotlin의 `out T`
Java의 ? `super T` → Kotlin의 `in T`

## ✅ 코드 예제

```kotlin
interface Fruit

class Apple : Fruit
class Banana : Fruit
class GrannySmith : Apple
```
```kotlin
class Shoe
class IPhone
```
```kotlin
class FruitHelper {
    fun eatAll(fruits: Collection<out Fruit>) {
        for (fruit in fruits) {
            println("Eating: $fruit")
        }
    }

    fun addApple(apples: MutableCollection<in Apple>) {
        apples.add(Apple())
        apples.add(GrannySmith())
    }
}
```
```kotlin
fun main() {
    val fruitHelper = FruitHelper()

    val fruits: MutableList<Fruit> = mutableListOf(Apple(), Banana())
    fruitHelper.eatAll(fruits)       // ✅ OK: List<Fruit>는 Collection<out Fruit>
    fruitHelper.addApple(fruits)     // ✅ OK: List<Fruit>는 MutableCollection<in Apple>

    val apples: MutableList<Apple> = mutableListOf(GrannySmith())
    fruitHelper.addApple(apples)     // ✅ OK: MutableList<Apple>는 MutableCollection<in Apple>

    val objects: MutableList<Any> = mutableListOf()
    fruitHelper.addApple(objects)    // ✅ OK: MutableList<Any>는 MutableCollection<in Apple>
    objects.add(Shoe())              // ✅ OK:
    objects.add(IPhone())            // ✅ OK: 
}
```

## ✅ out T — "T 또는 T의 하위 타입"
- 읽기 전용: 안전하게 값을 꺼낼 수 있지만, 값을 넣을 수는 없음
- 예제:
```kotlin
fun eatAll(fruits: Collection<out Fruit>)
```

- Apple, Banana, GrannySmith 등 Fruit을 구현한 모든 타입을 받을 수 있음
- fruits.forEach { ... } 가능
- fruits.add(...)는 불가능

## ✅ in T — "T 또는 T의 상위 타입"
- 쓰기 전용: T 타입을 안전하게 넣을 수 있지만, 꺼낼 때는 Any?로 다뤄야 함
- 예제:
```kotlin
fun addApple(apples: MutableCollection<in Apple>)
```

- Apple, Fruit, Any 타입의 컬렉션을 받을 수 있음
- apples.add(Apple()), apples.add(GrannySmith()) 가능
- val apple: Apple = apples.first()는 불가능

### 🔍 예제 흐름 분석
```kotlin
val fruits: MutableList<Fruit> = mutableListOf(Apple(), Banana())
fruitHelper.eatAll(fruits)       // ✅ OK: Collection<out Fruit>
fruitHelper.addApple(fruits)     // ✅ OK: MutableCollection<in Apple>

val apples: MutableList<Apple> = mutableListOf(GrannySmith())
fruitHelper.addApple(apples)     // ✅ OK: MutableCollection<in Apple>

val objects: MutableList<Any> = mutableListOf()
fruitHelper.addApple(objects)    // ✅ OK: MutableCollection<in Apple>
objects.add(Shoe())              // ✅ OK
objects.add(IPhone())            // ✅ OK
```


## 📌 제네릭 와일드카드 요약 (Kotlin 버전)
| 키워드     | 방향       | 사용 목적     | 예시 메서드                             |
|------------|------------|----------------|------------------------------------------|
| `out T`    | 상향 (하위 타입 허용) | 읽기 전용     | `eatAll(fruits: Collection<out Fruit>)`  |
| `in T`     | 하향 (상위 타입 허용) | 쓰기 전용     | `addApple(apples: MutableCollection<in Apple>)` |



## 🧠 기억법: PECS 원칙
- Producer → out (생산자: 값을 꺼냄)
- Consumer → in (소비자: 값을 넣음)

---

