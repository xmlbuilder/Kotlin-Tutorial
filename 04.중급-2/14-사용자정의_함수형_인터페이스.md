# 함수형 인터페이스

아래는 Java로 작성된 사용자 정의 함수형 인터페이스 예제를 Kotlin 스타일로 변환한 코드입니다.  
Kotlin에서는 함수형 인터페이스 대신 함수 타입을 직접 사용할 수 있지만, Java와의 호환성을 위해  
`@FunctionalInterface` 에 해당하는 `fun interface` 도 제공합니다.

## ✅ Kotlin으로 변환된 코드
```kotlin
// 함수형 인터페이스 정의
fun interface FunInterface {
    fun doSomething(text: String)
}
```
```kotlin
fun main() {
    // 람다식으로 구현체 할당
    val funInterface = FunInterface { text ->
        println(text)
    }

    // 메서드 호출
    funInterface.doSomething("Model")
}
```

## 📌 Kotlin에서의 특징
| 항목                     | 설명 또는 예시 코드                                      |
|--------------------------|----------------------------------------------------------|
| 함수형 인터페이스 선언   | `fun interface`                                          |
| 람다식 구현              | `FunInterface { text -> println(text) }`                |
| 메서드 호출              | `funInterface.doSomething("Model")`                     |
| 함수 타입 변수 선언      | `val f: (String) -> Unit = { println(it) }`             |
| 메서드 참조              | `val f = ::println`                                     |

- Kotlin에서는 함수형 인터페이스를 직접 정의하지 않고도 함수 타입을 변수에 할당해 사용할 수 있어 더 유연합니다.

---

