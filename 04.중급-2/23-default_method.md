# ✅ default method란?
Kotlin에서는 인터페이스에 메서드의 기본 구현을 포함할 수 있습니다.
이는 Java 8의 default 메서드와 유사한 개념이며, 기존 인터페이스를 깨지 않고 확장할 수 있도록 해줍니다.

## 📌 특징
- Kotlin 인터페이스는 메서드 본문을 포함할 수 있음 `(default 키워드 없이)`
- 인터페이스를 구현하는 클래스는 해당 메서드를 오버라이드할 수도 있고, 생략할 수도 있음
- 여러 인터페이스에서 동일한 시그니처의 메서드가 충돌할 경우, 반드시 오버라이드하여 명확히 지정해야 함

## 🔍 예제 코드 해석
### 1. Printable 인터페이스
```kotlin
interface Printable {
    fun printString() {
        println("default implementation")
    }
}
```
- printString()은 기본 구현을 가진 메서드
- 이 인터페이스를 구현하는 클래스는 오버라이드하지 않아도 사용 가능

### 2. OverrideDefault 클래스
```kotlin
class OverrideDefault : Printable {
    override fun printString() {
        println("override implementation")
    }
}
```

- Printable을 구현하고 printString()을 오버라이드
- 기본 구현을 무시하고 자신만의 로직을 제공

### 3. 실행 예제
```kotlin
fun main() {
    val obj = OverrideDefault()
    obj.printString() // 출력: override implementation
}
```

- OverrideDefault 객체를 생성하고 printString() 호출
- 오버라이드된 메서드가 실행됨

## 📦 전체 구조 요약

| 구성 요소         | 역할 또는 관계                          | 주요 기능 또는 메서드         |
|------------------|------------------------------------------|-------------------------------|
| `Printable`      | 인터페이스                               | `printString()` 기본 구현 포함 |
| `OverrideDefault`| `Printable` 구현                         | `printString()` 오버라이드     |
| `main()`         | 실행 함수                                | 오버라이드된 메서드 호출       |



## 🧪 확장 예제: 오버라이드하지 않은 경우
```kotlin
class UseDefault : Printable {
    // printString() 오버라이드하지 않음
}
```
```kotlin
fun main() {
    val obj = UseDefault()
    obj.printString() // 출력: default implementation
}
```
- Printable의 기본 구현이 그대로 사용됨
- 오버라이드하지 않아도 정상 동작

## ✨ Kotlin에서의 차이점 요약

| 항목                         | Java (default method)         | Kotlin 인터페이스 기본 구현         | 비고 또는 특징                          |
|------------------------------|-------------------------------|-------------------------------------|-----------------------------------------|
| 키워드 필요 여부             | `default` 키워드 필요          | 키워드 없이 바로 구현 가능           | Kotlin은 문법적으로 더 간결함           |
| 기본 구현 포함 방식          | 메서드에 `default` 선언        | 메서드 본문 직접 작성 가능           | 동일한 기능을 더 직관적으로 표현        |
| 다중 인터페이스 충돌 처리    | 명시적 오버라이드 필요         | `super<인터페이스>.메서드()` 사용 가능 | 충돌 시 명확한 호출 방식 제공           |
| 런타임 동작                  | JVM 기반                      | JVM 또는 Native 가능                 | Kotlin/Native에서도 동일하게 동작 가능  |

---


