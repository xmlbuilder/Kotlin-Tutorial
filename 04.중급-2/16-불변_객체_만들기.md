## 🧠 Kotlin 버전: 불변 배열 객체
- **불변 객체(Immutable Object)** 는 생성 이후 상태가 절대 변경되지 않는 객체입니다.
- 멀티스레드 환경에서 안전하고, 예측 가능한 동작을 보장합니다.

### ✅ 클래스 정의
```kotlin
class ImmutableIntArray(input: IntArray) {
    private val array: IntArray = input.copyOf()
    fun getValue(): IntArray = array.copyOf()
}
```
- copyOf()는 Kotlin에서 배열을 깊은 복사하는 표준 메서드입니다.
- `val` 은 Kotlin에서 final과 같은 역할을 하며, 재할당이 불가능합니다.

### ✅ 테스트 코드
```kotlin
fun main() {
    val values = intArrayOf(1, 2, 3, 4)
    val immutableIntArray = ImmutableIntArray(values)
    val newValues = immutableIntArray.getValue()

    println("Original: ${values.joinToString()}")
    println("Copied:   ${newValues.joinToString()}")
}
```

- joinToString()을 사용하면 배열을 보기 좋게 출력할 수 있음.
- values와 newValues는 값은 같지만 서로 다른 배열입니다.


## 🔒 Kotlin에서 불변 객체를 만드는 핵심 규칙

| 항목             | Kotlin 방식                                      | 설명                                      |
|------------------|--------------------------------------------------|-------------------------------------------|
| 필드 선언        | `private val array`                              | `val`은 재할당 불가, `private`로 외부 차단 |
| 생성자에서 복사  | `input.copyOf()`                                 | 배열을 깊은 복사하여 참조 보호            |
| Getter에서 복사  | `getValue() = array.copyOf()`                    | 외부에 복사본만 제공하여 내부 상태 보호   |
| 리스트 복사      | `list.toList()` 또는 `list.toImmutableList()`    | 불변 리스트 생성 (Kotlin 1.9+ 지원)        |


### 🧪 확장 예제: 불변 리스트
```kotlin
class ImmutableListWrapper(input: List<String>) {
    private val list: List<String> = input.toList()

    fun getList(): List<String> = list.toList()
}
```

- toList()는 원본 리스트를 복사해서 불변 리스트로 만듭니다.
- Kotlin 1.9 이상에서는 toImmutableList()도 사용할 수 있음.

---

