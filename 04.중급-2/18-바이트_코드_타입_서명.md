# 바이트 코드 타입 서명
아래는 Java 바이트코드 타입 서명에 대한 내용을 Kotlin 관점에서 재구성한 버전입니다.  
Kotlin도 JVM 위에서 동작하기 때문에 바이트코드 타입 서명은 동일하게 적용되며,  
Kotlin 컴파일러 역시 .class 파일을 생성할 때 같은 타입 서명을 사용합니다.

## 🧠 바이트코드 타입 서명
Kotlin은 JVM 기반 언어이므로 Java와 동일한 바이트코드 타입 서명 규칙을 따릅니다.
이 서명은 .class 파일 내부에서 타입을 표현하는 방식이며, 리플렉션이나 바이트코드 조작 시 사용됩니다.

## 🔍 각 타입 서명의 의미

| 타입 서명       | Java 타입         | 예시 또는 설명                                 |
|----------------|-------------------|------------------------------------------------|
| `Z`            | `boolean`         |                                                |
| `B`            | `byte`            |                                                |
| `C`            | `char`            |                                                |
| `S`            | `short`           |                                                |
| `I`            | `int`             |                                                |
| `J`            | `long`            |                                                |
| `F`            | `float`           |                                                |
| `D`            | `double`          |                                                |
| `L...;`        | 참조형 클래스     | `Ljava/lang/String;` → `String` 클래스         |
| `[type`        | 배열              | `[I` → `int[]`, `[Ljava/lang/String;` → `String[]` |
| `(args)ret`    | 메서드 시그니처   | `(I)Z` → `boolean func(int)`                   |

## 📌 예시 해석
- Ljava/lang/String; → String
- [I → IntArray
- [[D → Array<DoubleArray>
- (Ljava/lang/String;I)V → fun method(msg: String, i: Int): Unit
- (I)Z → fun method(i: Int): Boolean
- ()V → fun method(): Unit

## 🛠️ 어디에 쓰이나요?
- Kotlin 컴파일러가 .class 파일을 생성할 때
- 리플렉션 API (KFunction, KClass) 내부에서
- 바이트코드 조작 라이브러리 (예: ASM, ByteBuddy)
- 디컴파일러나 클래스 분석 도구

## 🔍 JVM 타입 서명 해석

| Kotlin 함수 시그니처                         | JVM 타입 서명                                      | 설명 요약             |
|---------------------------------------------|---------------------------------------------------|------------------------|
| fun sum(a: Int, b: Int): Int                | (II)I                                             | Int, Int → Int         |
| fun echo(msg: String): String               | (Ljava/lang/String;)Ljava/lang/String;           | String → String        |
| fun store(fruit: Fruit)                     | (LFruit;)V                                        | Fruit → Unit           |
| fun isEmpty(list: List<String>): Boolean    | (Ljava/util/List;)Z                               | List → Boolean         |
| fun matrix(): Array<DoubleArray>            | ()[[D                                             | → Array<DoubleArray>   |


## 📌 구성 규칙 요약
- 기본 타입: I → Int, Z → Boolean, D → Double, V → Unit
- 참조 타입: L패키지/클래스명; → 예: Ljava/lang/String;

---
