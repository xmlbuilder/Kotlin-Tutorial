# 🧠 Kotlin Set 구조 요약
## 📌 Set 자료 구조란?
- 중복을 허용하지 않는 데이터 집합
- 기본적으로 순서가 보장되지 않음 (HashSet)
- 수학적 집합 개념을 기반으로 함

## 📦 Kotlin Collection 인터페이스
- Collection<T>은 Iterable<T>를 상속
- 주요 하위 인터페이스: List, Set, Map
- 데이터를 그룹으로 다루기 위한 공통 메서드 제공

## 📦 Set 인터페이스
- Collection을 확장한 인터페이스
- 중복 없는 요소 집합을 표현
- 주요 구현체: HashSet, LinkedHashSet, SortedSet(TreeSet)

## 📦 Set 주요 메서드 요약

| 메서드               | 설명                                                                 |
|----------------------|----------------------------------------------------------------------|
| `remove(element)`     | 지정된 요소를 Set에서 제거                                           |
| `removeAll(elements)` | 주어진 컬렉션에 포함된 모든 요소를 제거                             |
| `retainAll(elements)` | 주어진 컬렉션에 포함된 요소만 유지하고 나머지는 제거                 |
| `clear()`             | Set의 모든 요소 제거                                                 |
| `size`                | Set에 포함된 요소의 개수 반환                                        |
| `isEmpty()`           | Set이 비어 있는지 여부 반환                                          |
| `iterator()`          | Set의 요소를 순회할 수 있는 반복자 반환                              |
| `toTypedArray()`      | Set의 모든 요소를 지정된 타입의 배열로 변환                          |


## ✅ 주요 메서드 샘플 코드
### 1. remove
```kotlin
val fruits = mutableSetOf("apple", "banana")
fruits.remove("apple")
println(fruits) // [banana]
```

### 2. removeAll
```kotlin
val fruits = mutableSetOf("apple", "banana", "cherry")
fruits.removeAll(listOf("banana", "cherry"))
println(fruits) // [apple]
```

### 3. retainAll
```kotlin
val fruits = mutableSetOf("apple", "banana", "cherry")
fruits.retainAll(listOf("banana", "kiwi"))
println(fruits) // [banana]
```

### 4. clear
```kotlin
val fruits = mutableSetOf("apple", "banana")
fruits.clear()
println(fruits) // []
```

### 5. size
```kotlin
val fruits = setOf("apple", "banana", "cherry")
println(fruits.size) // 3
```

### 6. isEmpty
```kotlin
val fruits = emptySet<String>()
println(fruits.isEmpty()) // true
```

### 7. iterator
```kotlin
val fruits = setOf("apple", "banana", "cherry")
for (fruit in fruits) {
    println(fruit)
}
```

### 8. toTypedArray
```kotlin
val fruits = setOf("apple", "banana")
val arr = fruits.toTypedArray()
println(arr.joinToString()) // apple, banana
```


## 🧩 HashSet

| 항목                   | 설명                                                                 |
|------------------------|----------------------------------------------------------------------|
| 구현 방식              | 해시 테이블 기반                                                    |
| 순서                   | 저장 순서 보장되지 않음                                             |
| 시간 복잡도            | 평균 O(1) (추가, 삭제, 검색)                                        |
| 중복 허용 여부         | 중복 불허                                                           |
| 비교 기준              | `hashCode()`와 `equals()` 기반으로 요소의 고유성 판단               |
| Null 허용 여부         | `null` 1개 허용 가능                                                |
| 용도                   | 유일성만 중요하고 순서가 필요 없는 경우                              |


## 🧩 LinkedHashSet

| 항목                   | 설명                                                                 |
|------------------------|----------------------------------------------------------------------|
| 구현 방식              | 해시 테이블 + 연결 리스트                                            |
| 순서                   | 삽입 순서 유지                                                       |
| 시간 복잡도            | 평균 O(1) (추가, 삭제, 검색)                                        |
| 중복 허용 여부         | 중복 불허                                                           |
| Null 허용 여부         | `null` 1개 허용 가능                                                |
| 용도                   | 유일성과 함께 순서가 중요한 경우                                     |


## 🌳 SortedSet

| 항목                   | 설명                                                                 |
|------------------------|----------------------------------------------------------------------|
| 구현 클래스            | `java.util.TreeSet` (Kotlin에서 직접 사용 가능)                      |
| 정렬 기준              | 기본 정렬 (`Comparable`) 또는 사용자 정의 `Comparator` 지정 가능     |
| 순서 유지 방식         | 입력 순서가 아닌 값의 정렬 순서로 유지                               |
| 시간 복잡도            | 평균 O(log n) (삽입, 삭제, 검색)                                     |
| 중복 허용 여부         | 중복 불허                                                           |
| Null 허용 여부         | `null` 요소는 허용되지 않음 (비교 불가능하므로 예외 발생)             |
| 용도                   | 정렬된 집합이 필요할 때                                              |

```kotlin
val set = sortedSetOf(10, 5, 15, 1, 6, 11, 16)
println(set) // [1, 5, 6, 10, 11, 15, 16]
```


## 📦 Set 구현체 비교 (Kotlin 기준)

| 구현체           | 순서 유지 여부       | 정렬 여부         | 성능 (검색/삽입/삭제) | 내부 구조             | 정렬 기준                     |
|------------------|----------------------|--------------------|------------------------|------------------------|-------------------------------|
| `HashSet`        | ❌ (순서 없음)        | ❌ (정렬 안 됨)     | `O(1)` 평균            | 해시 테이블            | 없음                          |
| `LinkedHashSet`  | ✅ (입력 순서 유지)   | ❌ (정렬 안 됨)     | `O(1)` 평균            | 해시 테이블 + 링크드 리스트 | 없음                          |
| `SortedSet`      | ❌ (입력 순서 무시)   | ✅ (자동 정렬됨)    | `O(log n)`             | 레드-블랙 트리         | `Comparable` 또는 `Comparator` |


### 🧪 실전 예제: 중복 제거 + 정렬
```kotlin
fun main() {
    val input = listOf(30, 20, 20, 10, 10)

    val hashSet = input.toSet()
    println("HashSet: $hashSet") // 순서 무관

    val linkedSet = LinkedHashSet(input)
    println("LinkedHashSet: $linkedSet") // 입력 순서 유지

    val treeSet = sortedSetOf(*input.toTypedArray())
    println("TreeSet: $treeSet") // 자동 정렬
}
```
---




